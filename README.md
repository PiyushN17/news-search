# news-search
A JavaScript solution for dynamically removing all child elements from a parent container using multiple DOM manipulation techniques (innerHTML, removeChild(), replaceChildren()). Integrated with the NewsAPI  to fetch and display dynamic news content.
